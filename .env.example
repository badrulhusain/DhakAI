# Paths are relative to the project root, or absolute. Defaults work with demo:setup.
TARGET_REPO=.runtime/demo-repo
WORKTREE_ROOT=.runtime/worktrees
CODEX_EXECUTABLE=codex
PORT=4000
ALLOWED_ORIGINS=http://127.0.0.1:3000,http://localhost:3000
# For a different backend port, set NEXT_PUBLIC_BACKEND_URL in apps/web/.env.local.
# Server-owned explanation JSON files, stored outside target worktrees.
DATA_ROOT=.runtime/data/explanations
